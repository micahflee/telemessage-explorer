[project]
name = "crunch"
version = "0.1.0"
description = ""
authors = [{ name = "Micah Lee", email = "micah@micahflee.com" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["click (>=8.2.1,<9.0.0)", "psycopg2 (>=2.9.10,<3.0.0)"]

[tool.poetry]

[tool.poetry.group.dev.dependencies]
mypy = "^1.15.0"
ruff = "^0.11.10"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
crunch = 'crunch:main'
